```diff

@@ clickshare-warranty-check @@
 test scripts to validate functionality of the web application

+ How to run the script

1. install cypress on your machine
2. clone the code
3. enter folder - cd clickshare-warranty-check
4. run command  - npx cypress run --browser chrome --spec "cypress/e2e/spec.cy.js"

+ Check test result

1. when you run the test on command line, you should get thes test results
2. or you can open the test result file to review -  cypress/results/test-output.xml
3. or you can open the recored video to check the whole testing flow - cypress/videos/spec.cy.js.mp4

